[project]
name = "media-bridge"
version = "0.1.0"
description = "Python CLI for video downloads using youtube-dl and upload them to cloud"
authors = [
    {name = "Jvillegasd",email = "johnnyvillegaslrs@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
